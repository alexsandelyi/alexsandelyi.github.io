# 조작과 플레이 감

**상태: 현실 계측치 전환 3단계 반영** (2026-08-02).

가장 먼저 구현한다. 규칙을 아무리 정교하게 만들어도 공을 다루는 느낌이
없으면 의미가 없고, 태클은 파울(`match-rules.md`)의 전제조건이라 순서상으로도
앞에 온다.

## 공 소유 상태

`baseline.md` 에 적은 대로 `ball.owner` 는 선언만 되어 있다. 이 필드를 살린다.

선수 반경 안에 공이 들어오고 **상대 속도가 낮으면** 트래핑 — 공이 선수 앞
일정 거리에 붙어 함께 움직인다.

```
if (dist < TRAP_R && relSpeed < TRAP_V) ball.owner = p
```

| 규칙 | 값(제안) | 근거 |
|---|---|---|
| 소유 중 최고속도 | `P_MAX × 0.88` | 드리블의 비용. 공 없는 수비수가 따라잡을 근거 |
| 재소유 쿨다운 | 0.35초 | 차고 곧바로 자기가 다시 잡으면 패스가 무의미해진다 |
| 소유 해제 | 태클 성공 / 강한 접촉 / 킥 | — |

`collide()`는 "소유 중이면 붙이고, 아니면 경합"으로 바뀌었다.
`TRAP_V=620`, `REOWN_CD=0.35`이며 측정 결과는 `balance.md`에 있다.

## 조준

슛·패스 방향을 **스틱/키 입력 방향**으로 정한다.

입력이 없으면 현재처럼 골문 자동 조준으로 폴백한다. 모바일에서 이동과
조준을 동시에 하기 어렵기 때문이다.

- 조준에 정확도 오차를 넣는다. 능력치(`team-tactics.md`)와 연결되고,
  먼 거리 슛이 자동으로 어려워진다.
- `aimAt()` 은 AI 전용으로 남는다.

## 액션

현재 2개(슛/패스) → 4개.

| 액션 | 키보드 | 내용 |
|---|---|---|
| 슛 | Space | 길게 누르면 최대 1.05초 동안 파워 충전 |
| 짧은 패스 | Shift | 가장 좋은 동료에게. 현재 `passTarget()` 개선 |
| 스루패스 | Ctrl | 동료의 **진행 방향 앞 공간**으로 |
| 태클 | Space (공 없을 때) | 슛 버튼 재사용 |

**이 표는 1단계 설계 시점 것이고 지금 키와 다르다.** Shift·Ctrl 은 Windows
고정 키 때문에 버렸고(2026-08-03), 사람의 스루패스 자리는 센터링이 됐다
(2026-08-05). 현재 배치는 [practice-mode.md](practice-mode.md) 「키 배치」가
정본이다.

`passTarget()` 은 현재 "앞쪽 40단위 이상, 거리 120 이상" 만 본다.
상대 위치를 보지 않아 수비수 정면으로 패스한다. 패스 경로상의 상대를
비용으로 넣는다.

### 모바일 버튼

버튼 4개는 엄지에 과하다. **공 소유 여부로 버튼 2개의 의미를 바꾼다.**

```
소유 중   [ 슛 ] [ 패스 ]
비소유    [ 태클 ] [ 선수전환 ]
```

라벨을 화면에서 함께 바꿔 표시한다. 바뀌는 것이 보이지 않으면 혼란만 준다.

스루패스는 모바일에서 패스 0.42초 롱프레스로 대체한다. 사람이 조작하는
선수의 스루패스는 지금 센터링이므로, 롱프레스가 곧 띄우는 패스다.

## 수동 선수 전환

전환은 키보드 Q(2P는 O), 모바일 비소유 상태의 보조 버튼이다.
누를 때마다 공 기준 가까운 순서로 순환한다.

자동 전환(`pickControlled()`)은 유지하되, **수동으로 잡은 직후
약 1.2초는 자동 전환을 막는다.** 그러지 않으면 눌러도 곧바로 되돌아간다.

## 스프린트와 체력

키보드 기본 이동은 달리기(20km/h), R(2P는 P)을 함께 누르면 걷기(5km/h),
E(2P는 U)를 함께 누르면 스프린트(최대 34.2km/h)다. 모바일은 스틱 크기를
0~최고속도에 연속 대응시킨다.

AI도 상황에 따라 걷기·조깅·달리기·스프린트를 고르고 같은 체력 소모를 받는다.
걷기와 조깅은 공통 속도이고 달리기 이상은 선수 속도 능력치가 반영된다.
체력이 떨어지면 후반 이동 속도가 낮아지며, 거의 소진되면 스프린트를 달리기로
제한한다. 판정 방법과 목표는 `movement-metrics.md`를 따른다.

## 검증 기준

- 소유 → 드리블 → 패스 → 재소유가 끊기지 않고 이어진다.
- 스틱 방향과 슛 방향이 일치한다 (세로 화면 회전 포함).
- 수동 전환 직후 자동 전환이 선수를 뺏지 않는다.
- 시뮬레이션 재측정에서 목표 승률·득점 복원
  (`node tools/soccer-sim.js -n 200`). 득점 목표는 `principles.md` 가
  기준이다 — 현실 계측치 전환으로 3분 5~6골에서 10분 2~3골로 바뀌었다.

## 남긴 결정

- 트래핑 거리는 선수·공 반경 합에 7단위를 더한 고정값이다.
- 모바일 슛은 버튼을 누르는 동안 충전하며 왼쪽 스틱 이동은 계속 가능하다.
