# Codex 위임 프롬프트

동네 축구 구현을 Codex 에 맡길 때 쓰는 프롬프트 모음.

## 쓰는 법

**한 번에 한 단계만 보낸다.** 6단계를 한 프롬프트에 몰아넣으면 Codex 가
앞 단계를 대충 끝내고 넘어간다. 단계가 끝나면 결과를 확인하고 다음
프롬프트를 새 세션으로 보낸다.

Codex 는 저장소의 `AGENTS.md` 를 자동으로 읽는다. 루트와 `games/soccer/`
양쪽에 있으므로 배포 규칙·좌표계·문서 규칙은 프롬프트에 다시 적지 않아도
된다. **단 `git push` 금지만은 프롬프트에서도 반복한다** — 되돌릴 수 없는
동작이라 한 번의 누락이 그대로 사고가 된다.

아래 `<공통 블록>` 은 모든 단계에 그대로 붙인다. `<task>` 만 갈아끼운다.

---

## 공통 블록 (모든 프롬프트에 붙인다)

```xml
<action_safety>
git push 를 절대 하지 않는다. 원격 저장소에 어떤 것도 올리지 않는다.
브랜치를 만들어 올리는 것도 금지다 — 저장소가 Public 이라 작업 브랜치도
소스 공개에 해당한다. 로컬 커밋은 자유롭게 해도 된다.

변경 범위는 games/soccer/ 와 tools/ 로 한정한다.
루트 index.html 과 일빵-런처-확정안.html 은 외부 도구가 재생성하는
번들이므로 건드리지 않는다. assets/ 와 app-ads.txt 도 건드리지 않는다.

관련 없는 리팩터링, 이름 변경, 코드 정리를 하지 않는다.
파일을 지우기 전에 현재 코드에서 참조되는지 먼저 검색한다.
git reset --hard 같은 파괴적 명령을 쓰지 않는다.
</action_safety>

<missing_context_gating>
저장소 사실을 짐작하지 않는다. 필요한 내용은 파일을 직접 읽어 확인한다.
설계 문서에 없는 것을 새로 설계하지 말고, 정말 빠져 있으면 무엇이
비어 있는지 명시한다.
</missing_context_gating>

<verification_loop>
코드만 읽고 "될 것" 이라고 판단하지 않는다.

1. node tools/soccer-sim.js -n 200 을 돌려 승률과 경기당 득점을 확인한다.
   목표는 쉬움 80% / 보통 53% / 어려움 23%, 경기당 총 득점 5~6골.
   30경기는 95% 신뢰구간이 ±18%p 라 판정에 쓸 수 없다. 반드시 200 이상.
2. python -m http.server 8899 를 띄우고 실제 브라우저에서 플레이한다.
   데스크톱 가로와 모바일 세로(경기장 90° 회전)를 모두 확인한다.
   확인이 끝나면 서버 프로세스를 정리한다.
3. 자동 검증은 requestAnimationFrame 에 의존하지 않는다. 백그라운드 창에서
   초당 1회로 스로틀된다. step(1/60) 을 직접 반복 호출한다.

검사가 실패하면 그 상태로 보고하지 말고 고친 뒤 다시 검사한다.
</verification_loop>

<completeness_contract>
작업을 끝까지 해결하고 멈춘다. 처음 그럴듯한 답에서 멈추지 않는다.
구현이 설계 문서와 달라졌으면 같은 작업에서 문서를 갱신한다.
차이가 남은 상태로 완료를 선언하지 않는다.
</completeness_contract>

<default_follow_through_policy>
합리적인 저위험 해석을 골라 계속 진행한다. 루틴한 질문으로 멈추지 않는다.

다만 다음 경우에는 멈추고 물어본다.
- 밸런스를 3회 조정해도 목표 범위에 들어오지 않을 때
- 설계 문서끼리 충돌할 때
- games/soccer/index.html 이 3000줄을 넘어 파일 분할이 필요해질 때
- 되돌리기 어려운 동작이 필요할 때
</default_follow_through_policy>

<grounding_rules>
모든 주장을 실제 실행 결과나 읽은 파일에 근거해서 말한다.
추론을 사실처럼 말하지 않는다. 가설이면 가설이라고 표시한다.
밸런스 수치는 짐작으로 확정하지 않는다. 측정 조건과 결과를 함께 적는다.
</grounding_rules>

<structured_output_contract>
끝나면 다음만 보고한다. 긴 서두나 반복 요약을 넣지 않는다.

1. 구현한 것 — 항목별 한 줄
2. tools/soccer-sim.js -n 200 결과 표 (승률, 신뢰구간, 경기당 총 득점)
3. 브라우저에서 확인한 것 / 확인하지 못한 것
4. 설계 문서에서 바꾼 부분
5. 남은 문제, 다음 단계로 넘긴 것
</structured_output_contract>

<progress_updates>
진행 보고는 짧게, 결과 중심으로. 주요 단계 전환이나 막힌 지점만 알린다.
</progress_updates>
```

---

## 0단계 — speed 스윕 측정 (1단계 전에 한 번)

```xml
<task>
저장소 ilbbangsite. tools/soccer-sim.js 에 speed 스윕 기능을 추가하고
기준선을 측정한다.

먼저 games/soccer/README.md 의 목차를 따라 balance.md 와 principles.md 를
읽는다.

할 일:
1. tools/soccer-sim.js 에 --sweep-speed 옵션을 추가한다.
   LEVELS[1].speed 를 0.84~0.94 까지 0.01 간격으로 바꿔가며 각 200경기를
   돌려 승률 곡선을 낸다. 시드는 고정한다.
2. 결과를 games/soccer/balance.md 「계획된 측정」 절에 표로 기록하고,
   측정 조건(명령·시드·경기 수·날짜)을 함께 적는다.

목적: speed 가 0.87 근처에서 계단 함수처럼 작동한다는 기존 관찰이 지금도
유효한지 확인하는 것이다. 1단계 구현 후 같은 스윕을 돌려 비교할 기준선이
된다. 이 결과가 「난이도와 팀 강함의 축 분리」 결정에 직접 들어간다.

끝난 상태: 승률 곡선이 balance.md 에 기록되고, 계단이 있는지 없는지에
대한 판단이 근거와 함께 적혀 있다.
</task>
```

*(공통 블록에서 `verification_loop` 의 2번 브라우저 항목은 이 단계에
해당 없음 — 코드 변경이 도구에만 있다.)*

---

## 1단계 — 조작·플레이 감

```xml
<task>
저장소 ilbbangsite. 동네 축구 게임의 1단계(조작·플레이 감)를 구현한다.
작업 대상은 games/soccer/index.html 한 파일이다.

먼저 games/soccer/README.md 를 읽고 목차를 따라 principles.md, baseline.md,
input-system.md, balance.md, roadmap.md 를 읽는다.
설계는 이미 확정돼 있다. 새로 설계하지 말고 input-system.md 에 적힌 것을
구현한다.

구현 항목 6개:
1. 공 소유 상태 — ball.owner 를 살린다(현재 선언만 되고 쓰이지 않는다).
   선수 반경 안에 공이 들어오고 상대 속도가 낮으면 트래핑.
   소유 중 최고속도 ×0.88, 재소유 쿨다운 0.35초.
2. 조준 — 슛·패스 방향을 스틱/키 입력 방향으로 정한다.
   입력이 없으면 기존 aimAt() 자동 조준으로 폴백한다.
   세로 화면에서 입력 벡터 회전이 조준에도 적용되는지 반드시 확인한다.
3. 액션 4개 — 슛(파워 충전), 짧은 패스, 스루패스, 태클.
   passTarget() 이 상대 위치를 보지 않아 수비수 정면으로 패스하는 문제를
   함께 고친다.
4. 모바일 버튼 — 공 소유 여부로 버튼 2개의 의미와 라벨을 바꾼다.
   소유 중 [슛][패스], 비소유 [태클][선수전환].
5. 수동 선수 전환 — 공 기준 가까운 순서로 순환. 수동 전환 직후 약 1.2초는
   자동 전환(pickControlled)을 막는다.
6. 스프린트 — 속도 ×1.25. 체력 소모는 이번 단계에서 0으로 둔다
   (3분 경기에서 유의미한지 미정. input-system.md 참조).

주의: collide() 를 전면 재작성하게 된다. 공 경합 방식이 바뀌므로
balance.md 의 기존 수치는 전부 무효가 된다. 재측정이 필수다.

끝난 상태:
- node tools/soccer-sim.js -n 200 이 목표 범위 안
  (쉬움 80% / 보통 53% / 어려움 23%, 경기당 총 5~6골)
- 브라우저에서 6개 항목이 모두 실제로 동작
- 구현이 input-system.md 와 달라진 부분은 문서를 갱신
- balance.md 에 1단계 후 측정 결과를 조건과 함께 추가
</task>
```

---

## 2단계 — 경기 규칙 (아웃·세트피스·전후반)

```xml
<task>
저장소 ilbbangsite. 동네 축구 게임의 2단계를 구현한다.
1단계(조작)가 끝난 상태를 전제로 한다.

games/soccer/README.md 목차를 따라 match-rules.md 를 중심으로 읽는다.

구현 항목:
1. 아웃오브플레이 — 터치라인 벽 반사를 제거하고 스로인/골킥/코너킥으로
   재개한다. collide() 에서 ball.lastTouch 를 기록한다.
2. 세트피스 상태 기계 — state 에 'setpiece' 를 추가하고 스로인·골킥·
   코너킥·프리킥·킥오프를 하나의 흐름으로 처리한다.
   resetPositions() 를 킥오프 전용에서 세트피스 배치 함수로 일반화한다.
   대기 시간은 match-rules.md 의 표를 따른다.
3. 전·후반제 — 90초 × 2, 하프타임에 코트 체인지.
   side===0 을 방향으로 쓰는 코드(homePos, aimAt, gkTarget, passTarget,
   색상 배정)를 팀별 attackDir 필드로 걷어낸다.
4. 경기 길이 설정 3분 / 6분 / 10분, 기본 3분.
5. 추가시간 — 세트피스로 멈춘 시간을 누적해 각 반 끝에 더한다.

오프사이드와 파울은 이 단계에 넣지 않는다. 3단계다.

끝난 상태:
- 아웃 판정 후 재개까지 흐름이 끊긴 느낌이 없다
- 세트피스에서 선수가 겹치거나 경기장 밖으로 나가지 않는다
- 코트 체인지 후 양 팀이 올바른 골문을 공격한다
- 밸런스 재측정 결과가 목표 범위 안
</task>
```

---

## 3단계 — 오프사이드·파울

```xml
<task>
저장소 ilbbangsite. 동네 축구 게임의 3단계를 구현한다.
2단계(세트피스)가 끝난 상태를 전제로 한다.

games/soccer/match-rules.md 의 「오프사이드」와 「파울과 카드」를 따른다.

구현 항목:
1. 오프사이드 — 패스를 차는 순간 판정. 난이도 연동(쉬움=끄기,
   보통·어려움=켜기). 판정 시 오프사이드 라인을 잠깐 그려 이유를 보여준다.
   우리 팀 AI 동료가 오프사이드를 피해 움직이도록 slotTarget() 에 수비 라인
   인식을 넣는다. 이게 없으면 플레이어가 통제 못 하는 이유로 공격이
   계속 끊긴다.
2. 파울 — 태클이 공보다 선수를 먼저 맞히면 프리킥, 페널티 에어리어 안이면
   PK. 속도·각도로 심각도를 매겨 무경고 / 옐로를 구분한다.

레드카드(퇴장)는 넣지 않는다. 인원 가변 처리라 4단계 포메이션 테이블화
이후로 미뤘다.

끝난 상태:
- 오프사이드 판정이 경기당 3회 이하
- 판정 이유가 화면에서 보인다
- 밸런스 재측정 결과가 목표 범위 안
</task>
```

---

## 4단계 — 팀·전술

```xml
<task>
저장소 ilbbangsite. 동네 축구 게임의 4단계를 구현한다.

games/soccer/team-tactics.md 를 따른다. balance.md 의 speed 스윕 결과를
먼저 확인한다 — 「난이도와 팀 강함의 축 분리」 결정이 그 데이터에 달려
있다. 계단이 남아 있으면 속도를 팀 능력치로 옮기는 순간 팀 강함도 계단이
된다. 판단 근거를 보고에 포함한다.

구현 항목:
1. 선수 능력치 5개 (속도·슛·패스·수비·체력) + 골키퍼 선방.
   값은 1~99, 화면에는 5단계 막대로만 표시한다.
2. 포메이션 테이블화 — 4-4-2 / 4-3-3 / 3-5-2 / 5-3-2.
3. 전술 슬라이더 — slotTarget() 의 상수를 노출한다.
   수비 라인 높이, 압박 강도, 폭, 추격 인원.
4. 팀 8~12개. 종합 등급은 선수 능력치에서 파생시킨다(직접 입력 금지).
5. 상성 — 별도 보정 계수를 곱하지 않는다. 기존 물리로 자연히 발생해야
   한다. 발생하지 않으면 상수 범위를 조정한다.

목표 승률은 평균 등급의 기준 팀끼리 붙었을 때로 재정의한다.

끝난 상태:
- 팀 등급차가 승률에 단조 반영된다
- 전술 조합별 봇 대 봇 승률 행렬에서 가위바위보가 관찰된다
- 능력치를 전부 동일하게 두면 이전 밸런스가 재현된다(회귀 확인)
- 측정 결과를 balance.md 에 기록
</task>
```

---

## 5단계 — 토너먼트 + 승부차기

```xml
<task>
저장소 ilbbangsite. 동네 축구 게임의 5단계를 구현한다.

games/soccer/game-modes.md 를 따른다.

구현 항목:
1. 승부차기 — 5번씩, 동률이면 서든데스. 기존 물리를 쓰고 조준과 골키퍼
   방향 선택만 추가한다.
2. 토너먼트 — 8팀 녹아웃 3라운드, 무승부 시 승부차기.
3. 팀 선택 화면 — 등급을 5단계 막대로 표시.
4. 저장 — localStorage 를 단일 키 ilbbang.soccer11.v2 로 정리한다.
   v1 의 난이도별 전적을 가져오는 마이그레이션 함수를 둔다.
   플레이어 기록을 날리지 않는다. v1 키는 지우지 않는다.
   접근은 전부 try/catch (사파리 프라이빗 모드에서 던진다).

끝난 상태:
- 토너먼트 3라운드를 완주할 수 있다
- 승부차기가 서든데스까지 동작한다
- 브라우저를 닫았다 열면 진행 상황이 복원된다
- v1 전적이 있는 상태에서 마이그레이션되고 기록이 보존된다
- localStorage 가 막힌 환경에서도 게임이 동작한다
</task>
```

---

## 6단계 — 리그·시즌·기록

```xml
<task>
저장소 ilbbangsite. 동네 축구 게임의 6단계를 구현한다.

games/soccer/game-modes.md 를 따른다.

구현 항목:
1. 리그 — 8팀 풀리그 14경기, 승점 순위표.
2. 시즌 — 리그 + 컵, 여러 시즌 누적.
3. 경기 후 기록 — 점유율(ball.owner 팀별 누적 시간), 슈팅/유효슈팅,
   코너, 파울/카드.

이 단계가 끝나면 전 단계 완료다. 최종 검증 후 사용자에게 변경 내용과
검증 결과를 보고한다. push 는 여전히 하지 않는다 — 사용자가 직접 승인한다.

끝난 상태:
- 리그 한 시즌을 완주할 수 있다
- 순위표가 승점·득실차로 정렬된다
- 경기 후 기록이 실제 이벤트와 일치한다
- 최종 밸런스 측정 결과가 목표 범위 안
</task>
```

---

## 단계가 끝날 때마다

Codex 보고를 받으면 다음을 직접 확인한다.

1. `git log --oneline` — 커밋이 로컬에만 있는지
2. `git status -sb` — `ahead` 만 있고 push 흔적이 없는지
3. `node tools/soccer-sim.js -n 200` — 보고된 숫자가 재현되는지
4. 브라우저에서 직접 플레이 — Codex 의 "확인했다" 를 그대로 믿지 않는다
5. `wc -l games/soccer/*.md` — 설계 문서 200줄 제한

---
---

# 리뷰 수정 프롬프트 (2026-08-01)

0~6단계 완료 후 코드 리뷰에서 나온 결함을 고치기 위한 프롬프트다.
위의 `공통 블록` 을 그대로 붙여 쓰되, 아래 `<regression_baseline>` 을
추가로 붙인다.

**순서를 지킨다.** A 를 먼저 한다 — 측정 도구가 고장난 상태에서 다른
수정을 검증하면 결과를 믿을 수 없다.

| 순서 | 프롬프트 | 내용 | 재측정 |
|---|---|---|---|
| A | 측정 도구 복구 | strict 모드, 죽은 speed 스윕 | 필수 (기준선 갱신) |
| B | 경기 상태 초기화 | 옐로카드·체력이 골마다 리셋 | 필수 |
| C | 오프사이드 실동작 | 판정이 경기당 0회 | 필수 |
| D | 난이도 축 정리 | 비단조 1P 보조 버프 | 필수 (전면 재튜닝) |
| E | 잔여 정리 | 통계·성능·죽은 코드 | 확인만 |

## 추가 블록 (A~E 모두에 붙인다)

```xml
<regression_baseline>
현재 기준선 — node tools/soccer-sim.js -n 200 --seed 1 (2026-08-01):

  난이도  승/무/패      승률 (95% CI)        득점  실점  총득점
  쉬움    164/17/19    82.0% (76.1~86.7)   4.38  1.98   6.36
  보통    113/37/50    56.5% (49.6~63.2)   2.65  2.04   4.70
  어려움   47/34/119   23.5% (18.2~29.8)   2.16  3.13   5.29
  전체 경기당 총 득점 5.45
  경기당 판정 offsides=0.00 fouls=2.50 cards=1.49

목표는 승률 80/53/23%, 경기당 총 득점 5~6골.

수정 후 같은 명령을 돌려 비교한다. 승률이 목표 신뢰구간을 벗어나면
그 수정 때문인지 난수 때문인지 --seed 를 바꿔 확인한다.
보고에는 수정 전후 수치를 나란히 적는다.
</regression_baseline>
```

---

## A. 측정 도구 복구 (먼저)

```xml
<task>
저장소 ilbbangsite. tools/soccer-sim.js 의 결함 두 가지를 고친다.
측정 도구가 잘못돼 있어 다른 수정의 검증이 성립하지 않는 상태다.

먼저 games/soccer/README.md 목차를 따라 principles.md, balance.md 를 읽는다.

결함 1 — 시뮬레이터가 게임을 strict 모드로 실행하지 않는다.
  tools/soccer-sim.js:392
    const full = 'Math.random = __rand;\\n' + src + '\\n' + HARNESS;
  games/soccer/index.html 의 스크립트는 'use strict' 로 시작한다.
  앞에 문장을 붙이면 지시어 프롤로그가 깨져 전체가 sloppy 모드로 돈다.
  브라우저는 strict, 시뮬레이터는 sloppy 로 같은 코드를 다르게 실행한다.
  strict 에서만 나는 오류는 200경기를 돌려도 잡히지 않는다.

  고치는 방향: runInContext 를 두 번 나눠 부른다. 먼저 시드 주입만
  실행하고, 그 다음 게임 소스와 하네스를 한 스크립트로 실행해
  'use strict' 가 첫 문장 자리를 지키게 한다.

  주의: strict 모드가 켜지면 하네스의 `__sim = {...}` 같은 암묵적 전역
  할당이 ReferenceError 를 낸다. 하네스도 함께 고쳐야 한다.
  또한 게임 코드에서 strict 위반이 새로 드러날 수 있다. 드러나면 고치고,
  무엇이 드러났는지 보고에 적는다.

결함 2 — --sweep-speed 가 죽은 필드를 스윕한다.
  games/soccer/index.html:238-240 의 LEVELS[].speed 는 선언만 있고
  게임 어디서도 읽히지 않는다. 4단계(커밋 6a4ac75)에서 p.speedMul 로
  대체되며 죽었다. 그런데 tools/soccer-sim.js 의 setLevelSpeed() 는
  여전히 이 필드를 쓴다. 지금 --sweep-speed 를 돌리면 아무것도 바꾸지
  않으므로 항상 "계단 없음" 이 나온다.

  고치는 방향: 살아 있는 축인 OPPONENT_SPEED_MUL(index.html:981,
  step() 1715 에서 실제로 읽힘)을 스윕하도록 바꾼다. 죽은
  LEVELS[].speed 필드는 제거한다. 도움말과 주석도 함께 고친다.

끝난 상태:
- 시뮬레이터가 strict 모드로 게임을 실행한다. 이를 검증하는 셀프테스트
  항목을 --self-test 에 추가한다.
- --sweep-speed 가 실제로 승률을 움직이는 축을 스윕한다.
- 새 스윕을 200경기로 돌려 결과를 balance.md 에 조건과 함께 기록한다.
- 기준선 재측정 결과를 수정 전 수치와 나란히 보고한다.
- LEVELS[].speed 제거가 baseline.md 의 난이도 표와 어긋나지 않게 갱신한다.
</task>
```

---

## B. 경기 상태 초기화

```xml
<task>
저장소 ilbbangsite. 옐로카드와 체력이 킥오프마다 지워지는 버그를 고친다.

games/soccer/index.html:599
  resetPositions() 안에서 p.yellow = 0; p.energy = 1; 을 실행한다.
  이 함수는 킥오프마다 호출된다 — 1658(하프타임), 1672(득점 후).
  따라서 누가 골을 넣는 순간 모든 경고가 사라지고 체력이 가득 찬다.
  p.yellow 는 1867 에서 카드 마커를 그리므로 화면에서도 사라진다.
  측정상 경기당 카드 1.49장이 나오는데 전부 휘발된다.

실제로 확인된 동작:
  경고·체력 설정 : { yellow: 1, energy: 0.2 }
  득점 후 킥오프 : { yellow: 0, energy: 1 }
  하프타임 후    : { yellow: 0, energy: 1 }

원인은 얕은 곳에 있다. resetPositions() 는 위치 재배치 함수인데 경기
전체에 걸친 상태(징계·체력)까지 건드린다. 킥오프마다 초기화돼야 하는
것과 경기 시작에만 초기화돼야 하는 것을 분리한다.

셀프테스트가 이걸 놓친 이유도 함께 고친다. tools/soccer-sim.js 의 카드
검사가 separateAll() 직후만 보고 킥오프를 거치지 않는다. 킥오프 뒤에도
경고가 남아 있는지 확인하는 항목을 추가한다.

끝난 상태:
- 옐로카드가 경기 내내 유지되고 다음 경기 시작에 초기화된다.
- 체력이 득점·하프타임으로 회복되지 않는다.
- 셀프테스트가 킥오프 통과 후의 경고 유지를 검증한다.
- 체력이 실제로 소모되면 승률이 움직인다. 재측정 결과를 보고한다.
</task>
```

---

## C. 오프사이드 실동작

```xml
<task>
저장소 ilbbangsite. 오프사이드가 실제 경기에서 한 번도 발동하지 않는
문제를 고친다.

200경기 측정에서 offsides=0.00/경기 다. 판정 경로는 셀프테스트에서
강제 배치로만 통과하고, 실제 플레이에서는 작동하지 않는다.

원인 — games/soccer/index.html:1493-1496
  slotTarget() 이 level > 0 일 때 MF·FW 를 뒤에서 두 번째 수비수 라인
  36단위 뒤로 하드 클램프한다. 그래서 AI 동료가 오프사이드 위치에
  갈 수 없다. markOffside() 는 패서의 '동료' 만 검사하므로, 사람이
  패스해도 동료가 전부 클램프돼 판정이 나오지 않는다.
  자동 대전만의 문제가 아니라 사람이 플레이해도 0회다.

match-rules.md 의 3단계 완료 기준은 "오프사이드 판정이 경기당 3회
이하" 였다. 0회는 규칙이 작동하는 것이 아니라 무력화된 것이다.
문서의 해당 서술도 현실에 맞게 고친다.

고치는 방향(제안이며 더 나은 방법이 있으면 근거와 함께 택한다):
  하드 클램프를 확률적·부분적 회피로 바꾼다. AI 가 라인을 존중하되
  가끔 넘어가게 두고, 넘어간 상태에서 패스를 받으면 판정이 나오게 한다.
  난이도가 높을수록 회피를 잘 하게 만들면 난이도 축과도 맞는다.

주의: 오프사이드가 실제로 발동하기 시작하면 공격이 끊기므로 득점이
줄어든다. 경기당 총 득점 5~6골을 유지하도록 함께 조정한다.

끝난 상태:
- 자동 대전 200경기에서 오프사이드가 경기당 0회 초과 3회 이하로 나온다.
- 브라우저에서 사람이 플레이할 때도 판정과 오프사이드 라인 표시를
  실제로 확인한다.
- 쉬움(level 0)에서는 여전히 판정이 나오지 않는다.
- 경기당 총 득점 5~6골 유지. 재측정 결과를 보고한다.
- match-rules.md 를 실제 동작에 맞게 갱신한다.
</task>
```

---

## D. 난이도 축 정리

```xml
<task>
저장소 ilbbangsite. 난이도가 AI 조정이 아니라 플레이어 몰래 버프로
구현돼 있는 것을 정리한다.

games/soccer/index.html:982-984
  const HUMAN_SHOOT_MUL    = [1.18, 1.40, 1.18];
  const HUMAN_TACKLE_BONUS = [0,    0.28, 0];
  const HUMAN_GK_REACH_MUL = [1,    1.35, 1];

쉬움과 어려움이 완전히 동일하고 보통만 크게 다르다. 난이도를 올리면
플레이어 슛이 1.40 → 1.18 로 약해지고 태클 보조와 골키퍼 반경 보너스가
함께 사라진다. 쉬움 → 보통에서는 반대로 플레이어가 강해진다.
난이도 축이 단조롭지 않다.

kickOwned():1271-1273 에도 1P 조작 선수만 골대 구석을 자동 조준하는
분기가 있다. 공유 물리에 사람 전용 특례가 겹겹이 얹혀 있어, 앞으로
밸런스를 건드릴 때마다 이 배열들을 함께 흔들어야 한다.

team-tactics.md 의 「난이도와 팀 강함의 축 분리」는 난이도 = AI 반응
(react/aimErr/gk), 팀 = 능력치로 확정했다. 지금 구현은 그 결정과 다르다.

고치는 방향:
  1순위 — 사람 전용 보조를 없애고 난이도를 AI 파라미터만으로 만든다.
  그것으로 목표 승률(80/53/23%)에 도달하지 못하면, 보조를 남기되
  난이도에 대해 단조롭게(쉬움이 가장 큰 보조, 어려움이 가장 작은 보조)
  만들고 왜 필요한지 balance.md 에 근거와 함께 남긴다.

이 작업은 전면 재튜닝이다. 한 번에 맞추려 하지 말고 한 축씩 바꾸며
200경기로 측정한다.

끝난 상태:
- 사람 전용 보조가 없거나, 난이도에 대해 단조롭다.
- 승률 80/53/23% 목표 신뢰구간 안, 경기당 총 득점 5~6골.
- baseline.md 의 난이도 파라미터 표와 team-tactics.md 의 축 분리 서술이
  실제 구현과 일치한다.
- 각 시도의 측정값을 balance.md 에 남겨 다음 사람이 재현할 수 있게 한다.
</task>
```

---

## E. 잔여 정리

```xml
<task>
저장소 ilbbangsite. 코드 리뷰에서 나온 작은 결함들을 정리한다.
게임 동작을 바꾸는 수정이 아니므로 밸런스는 확인만 한다.

games/soccer/index.html:
  1295 markOffside() 가 kickOwned() 에서만 호출된다. collide() 의 패스
       경로(1353, 1367)는 오프사이드 검사를 건너뛴다. 두 경로가 같은
       규칙을 따르게 한다.
  1201 PK 를 AI 가 차면 kind='pass' 로 들어와 ball.shotSide = -1 이 되고
       유효슈팅이 집계되지 않는다. 세트피스 종류가 penalty 면 슛으로
       집계한다.
  1029 offside 객체가 3초 유지되고, 같은 편 비위반자가 공을 받아도
       1097 에서 지워지지 않는다. 이후의 정당한 터치가 오판될 수 있다.
       패스가 다른 선수에게 정상 도달하면 해제되게 한다.
  1467 nearestChasers() 가 매 프레임 2회 배열 할당 + 정렬을 한다.
  1494 secondLastDefenderLine() 이 MF·FW 마다 호출되며 매번 map+sort 로
       11개 배열을 만든다. 두 곳 모두 한 프레임에 한 번만 계산해
       재사용한다. 200경기 측정 시간에 직접 영향이 있다.
  1172 takeSetpiece() 가 passTarget() 을 항상 계산하지만 penalty·corner·
       goalkick 에서는 쓰지 않는다. 필요한 분기에서만 계산한다.
  1193 s.type === 'throw' 분기는 1176 에서 이미 처리돼 도달할 수 없다.
  1798 C.gold 는 C 객체에 없는 키다. 폴백으로 동작하지만 오해를 부른다.
  1869 옐로카드 마커만 세로 화면에서 역회전하지 않는다. 등번호(1875)와
       조작 표시(1862)는 view.rot 일 때 회전한다. 같게 맞춘다.

끝난 상태:
- 위 항목이 모두 처리됐거나, 처리하지 않은 것은 이유가 적혀 있다.
- 성능 수정 후 200경기 측정 시간이 줄어든 것을 수치로 보고한다.
- 승률과 득점이 기준선에서 유의하게 벗어나지 않는다.
- 브라우저에서 세로 화면 카드 표시를 실제로 확인한다.
</task>
```

---

## 수정 후 확인

A~E 각각이 끝날 때마다 위의 「단계가 끝날 때마다」 5가지를 하고,
추가로 다음을 본다.

1. `node tools/soccer-sim.js --self-test` — 전 항목 통과
2. 수정 전후 승률을 나란히 비교 — 목표 신뢰구간 안인지
3. 골 넣은 뒤 옐로카드가 남아 있는지 (B 이후)
4. 실제 플레이에서 오프사이드 판정이 나오는지 (C 이후)
