# 일빵 게임 런처 — Codex 작업 규칙

이 파일은 저장소 루트와 모든 하위 경로에 적용한다.  
프로젝트는 정적 사이트이며 GitHub Pages의 `main` 브랜치 루트에서 배포된다.
기본 주소는 `https://alexsandelyi.github.io/`이고, 루트 `CNAME` 파일로
커스텀 도메인 `ilbbang.com`을 지정한다.

## 최우선 원칙

1. 사이트와 게임의 기존 동작을 보존한다.
2. 공개 배포와 소스 공개를 같은 위험 수준으로 취급한다.
3. 생성된 번들을 직접 임시 수정하는 대신 재현 가능한 작업 흐름을 남긴다.
4. 추측으로 완료를 선언하지 말고 실제 결과를 검증한다.

## 공개 배포 승인

- `git push`, GitHub Pages 배포 또는 다른 공개 호스팅 작업을 하기 전에
  반드시 사용자에게 명시적으로 확인받는다.
- 사용자의 승인은 해당 push 또는 배포 1회에만 유효하다.
- 이전 작업에서 받은 승인을 다음 작업에 재사용하지 않는다.
- 코드 수정, 로컬 검증과 로컬 커밋은 별도 승인을 요구하지 않는다.
- 저장소가 Public이므로 push하면 사이트와 소스가 즉시 공개될 수 있음을
  승인 요청에 짧게 알린다.
- 사용자가 별도로 요청하지 않는 한 GitHub Pages 이외의 호스팅으로
  배포하지 않는다.

## 축구 게임 공개 시점

- `games/soccer/`의 축구 게임은 개발 중간 상태를 원격 저장소나 공개
  사이트에 올리지 않는다.
- 기능 구현, 난이도 시뮬레이션, 반응형 레이아웃, 입력과 실제 플레이 검증을
  모두 마칠 때까지 수정과 커밋은 로컬에만 보관한다.
- Public 저장소의 중간 작업 브랜치도 소스 공개에 해당하므로 원격으로
  push하지 않는다.
- 축구 게임과 런처 연결까지 최종 검증한 뒤 변경 내용과 검증 결과를
  사용자에게 보고하고, 해당 1회의 `git push`와 GitHub Pages 공개 승인을
  명시적으로 받은 경우에만 완성본을 한 번에 올린다.
- 사용자가 개발 도중 공개 방식을 명시적으로 변경하지 않는 한 이 원칙을
  유지한다.

## 주요 파일과 디렉터리

| 경로 | 규칙 |
|---|---|
| `일빵-런처-확정안.html` | 로컬 작업 원본. Git 추적 제외 |
| `index.html` | 배포 산출물. 원본과 바이트 단위로 같아야 함 |
| `assets/clickable-v2/` | 현재 사용하는 클릭 글자 WebP 8개 |
| `assets/lettering-v1/` | 현재 사용하는 제목 글자 WebP 3개 |
| `assets/clickable-v1/` | 미참조 구버전. 배포 대상이 아님 |
| `app-ads.txt` | AdMob 인증 파일. 루트 위치를 유지하며 삭제 금지. 도메인 변경 시 AdMob 콘솔의 개발자 사이트 주소도 함께 변경 |
| `CNAME` | 커스텀 도메인 `ilbbang.com`. 삭제하면 도메인이 해제된다 |
| `games/soccer/` | 의존성 없는 Canvas 2D 축구 게임 |
| `games/soccer/js/` | 게임 코드 12개. 클래식 `<script src>` 순서 로드. 파일당 500줄 이하 |
| `games/soccer/assets/players.png` | 선수 스프라이트 시트 271칸(기존 포즈 15칸 + 이동 8방향×2프레임 + 액션 10종×8방향×3프레임). `tools/sprite-gen.py` 산출물. 손으로 고치지 않음 |
| `tools/sprite-src/` | 원본 일러스트와 방향·액션 프레임 소스. 저장소 밖 원본에 의존하지 않는다 |
| `games/soccer/CLAUDE.md` | 축구 작업 지침. **밸런스 기준선 수치와 스프라이트 실패 기록이 여기에만 있다** — 지우면 복원 불가 |
| `tools/` | 개발 도구. `soccer-sim.js` = 밸런스 측정, `sprite-gen.py` = 스프라이트 생성. 사이트 동작과 무관 |
| `tools/patches/` | 번들 재적용 패치 7개. 번들이 재생성되면 여기서 복구한다 |
| `tmp/` | 번들 추출물·스크린샷 등 버려도 되는 작업물. Git 추적 제외 |
| `.nojekyll` | GitHub Pages에 필요. 삭제 금지 |

## HTML 원본과 배포본

- `index.html`을 직접 편집하지 않는다.
- 먼저 `일빵-런처-확정안.html`을 수정하고 검증한 다음 `index.html`에
  바이트 그대로 복사한다.
- Windows PowerShell에서는 다음과 같이 동기화한다.

```powershell
Copy-Item -LiteralPath '.\일빵-런처-확정안.html' -Destination '.\index.html' -Force
```

- 동기화 후 두 파일의 SHA-256 해시가 같은지 확인한다.
- 작업 시작 전 원본의 파일 크기와 수정 시각을 확인한다.
- 에이전트가 수정하지 않았는데 작업 도중 원본이 바뀌었다면 덮어쓰지 말고
  사용자에게 먼저 알린다.
- 원본은 외부 도구가 다시 생성할 수 있다. 재생성 후에도 필요한 수정은
  `tools/patches/patch-*.js` 같은 재적용 가능한 패치로 남긴다.
  **`tmp/` 는 gitignore 라 거기 두면 이 PC 밖으로 안 나간다.**
- 새 기능은 가능하면 `games/` 같은 번들 밖의 독립 파일로 구현한다.

## 자체 압축 해제 번들 수정 규칙

`index.html`과 원본 HTML은 다음 두 블록을 사용하는 자체 압축 해제 번들이다.

- `<script type="__bundler/manifest">`: base64 또는 gzip 자산
- `<script type="__bundler/template">`: JSON 문자열로 저장된 실제 페이지

번들을 다시 포장할 때 아래 규칙을 모두 지킨다.

1. 템플릿과 manifest를 각각 JSON으로 정상 파싱할 수 있는지 확인한다.
2. `JSON.stringify()` 결과의 `</`를 반드시 `<\u002F` 형태로 이스케이프한다.
3. 코드 치환은 예상 매칭 수를 검증하고, 예상과 다르면 쓰기를 중단한다.
4. `Launcher.jsx`만 수정한다. 유사한 `Draft1~5.jsx`까지 전체 치환하지 않는다.
5. 자산을 제거할 때는 최종 템플릿에서 참조되지 않는 manifest 항목인지
   확인한다.
6. 수정 후 템플릿 JSON, manifest JSON과 외부 자산 경로를 다시 검증한다.

## 이미지와 글꼴

- `index.html`은 이미지까지 완전히 자립형인 파일이 아니다.
- `assets/clickable-v2/`와 `assets/lettering-v1/`의 WebP 11개를
  `index.html`과 함께 배포한다.
- 이미지 글자는 실제 텍스트 폰트가 아니라 이미지 생성 자산이다.
  클릭 대상의 접근 가능한 이름과 키보드 동작은 계속 유지한다.
- 사이트의 실제 텍스트는 다음 시스템 글꼴 스택을 사용한다.

```css
font-family:
  "Apple SD Gothic Neo",
  "Malgun Gothic",
  system-ui,
  sans-serif;
```

- 사용자가 명시적으로 요청하지 않는 한 웹폰트 파일을 다시 번들에 포함하지
  않는다.
- 새 글꼴을 포함해야 한다면 상업적 웹 사용과 재배포 조건을 확인하고
  필요한 라이선스 고지를 함께 추가한다.
- 검증용 JPEG, 단계별 백업 HTML과 미참조 구버전 자산은 배포에 포함하지 않는다.

## 반응형 레이아웃

- 주요 브레이크포인트는 `1023px`와 `767px`이다.
- `1023px` 이하에서는 히어로 영역을 세로로 쌓는다.
- `767px` 이하에서는 좌측 레일을 하단 탭바로 전환한다.
- 넓은 화면에서는 콘텐츠 읽기 영역만 최대 **1760px**로 제한한다.
  (2026-08-05 에 1440 → 1760. 1440 은 1920 화면에서 좌우 194px 씩 남아
  허전했다. 상한을 없애지는 않는다 — 미디어 높이 440px 상한 때문에
  초광폭에서 히어로가 2.7:1 로 납작해진다.)
- 배경, 레일과 탭바는 전체 창을 채우게 유지한다.
- 히어로가 16:9 비율로 무제한 커져 1920px 화면에서 과도하게 높아지지
  않도록 확인한다.

## 커뮤니티 게시판 규칙

- `community/`(정적 프런트)와 `worker/`(Cloudflare Worker)로 나뉜다.
  **Pages 는 `worker/` 를 서빙하지 않는다.**
- **아직 배포 전이다** (2026-08-13). `api.ilbbang.com` 연결과 CORS 확인이
  남았고, 수정·신고는 API 만 있고 화면이 없다.
- `js/01-api.js` 의 `API_BASE` 가 비어 있으면 localStorage 가짜 저장소로
  돈다. 지금이 그 상태다.
- **apex(`ilbbang.com`)에 Cloudflare 프록시를 켜지 않는다.** GitHub Pages
  인증서 갱신(90일마다 HTTP 검증)이 위험해진다. Worker 는
  `api.ilbbang.com` 서브도메인에 프록시를 켜서 붙이고 CORS 로 연결한다.
- 글 비밀번호 해시를 bcrypt 나 반복 PBKDF2 로 바꾸지 않는다. Workers 무료
  플랜의 호출당 CPU 10ms 한도를 넘긴다. SHA-256 + 소금 + 서버 후추를
  유지하고, 화면에 「쓰던 비밀번호를 넣지 마세요」를 띄운다.
- 관리자 비밀번호, Turnstile 비밀키, IP 소금값은 `wrangler secret put` 으로
  둔다. 저장소가 Public 이므로 코드에는 이름만 남긴다.
- **Turnstile 은 두 값을 같이 켜고 같이 끈다.** 공개값
  `TURNSTILE_SITE_KEY`(`community/js/01-api.js`)와 비밀값
  `TURNSTILE_SECRET`(`wrangler secret put`)이 짝이다. 둘 다 비면 검사를
  건너뛰는 로컬 개발, 둘 다 차면 운영이다. **비밀키만 넣으면 화면에 위젯이
  없어 토큰이 안 만들어지고 글쓰기가 100% 403 으로 막힌다** (2026-08-14
  까지 실제로 이 상태였다). 사이트 키만 넣으면 위젯만 뜨고 검증은 안 된다.
- 위젯 스크립트는 글쓰기 창을 처음 열 때만 받는다. 토큰은 한 번 쓰면
  소모되므로 창을 열 때와 올리기 실패 시 `reset()` 한다.
- 로컬에서 위젯을 보려면 Cloudflare 공개 시험 키
  `1x00000000000000000000AA`(항상 통과)를 잠깐 넣는다. 이 키는 늘 같은 더미
  토큰을 주므로 토큰이 매번 새로 나오는지는 이 방법으로 확인할 수 없다.
- 사용자 글을 `innerHTML` 에 넣지 않는다. `textContent` 만 쓰고 줄바꿈은
  `white-space: pre-wrap` 으로 살린다.
- 배포 없이 `node worker/test.mjs` 52항목으로 검증한다.

## 축구 게임 규칙

- 경기장 논리 좌표는 실제 105m × 68m를 1m = 20단위로 옮긴
  `2100 × 1360`이다.
- 물리, AI와 경기 판정은 항상 논리 좌표에서 계산한다.
- 화면 크기와 회전은 그리기 및 입력 변환 단계에서만 처리한다.
- 창 크기나 화면 방향이 경기 결과를 바꾸지 않게 한다.
- 세로 화면에서는 경기장을 90° 회전하며 입력 벡터도 같은 각도로 변환한다.
- 회전 변환의 `translate`에는 회전 전 좌표 순서 `(x, y)`를 사용한다.
- **수치 측정은 사용자가 직접 한다** (2026-08-05 지시). 시뮬레이션 배치,
  `soccer-lab.js`, 브라우저 실측 표를 먼저 돌리지 않는다. 요청받았을 때만
  실행하고 "측정해서 확인하겠다"고 제안하지 않는다.
- `--restamp`(`js/` 수정 시 필수)와 `--self-test`(통과/실패 검사)는 계속한다.
  둘은 수치를 뽑는 게 아니라 깨진 것을 잡는 장치다.
- 측정하지 않은 값은 **모른다고 적는다.** 짐작해서 단정하지 않는다.
- 승률·득점 목표에 맞춘 상수 튜닝(재밸런싱)은 하지 않는다.
- 도구는 유지한다. `tools/soccer-sim.js` 는 실제 `step(1/60)` 을 반복하며,
  난이도당 **200경기 이상**이 필요하다 — 30경기는 95% 신뢰구간이 ±18%p 라
  난이도 간 차이를 판정할 수 없다.
- 목표 승률 쉬움 80%, 보통 53%, 어려움 23%, 골목 FC끼리의 10분 기준
  경기당 총 득점 2~3골은 **참고값이지 강제값이 아니다.** 실측 기준선과 측정
  조건은 `games/soccer/CLAUDE.md` 의 「밸런스 기준선」에 있다.

## 설계 문서를 두지 않는다 (2026-08-13)

- `*.md` 설계 문서 24개를 지웠다. 코드를 직접 읽을 수 있는 규모이고, 낡은
  문서가 현재 사실인 척하는 쪽이 더 위험했다.
- **새 `.md` 를 만들지 않는다.** 알아야 할 사실은 규칙 문서 4개나 코드
  주석에 적는다.
- 지운 문서는 git 에 남아 있다 (`git show 267d550:games/soccer/balance.md`).
  꺼내 보되 현재 사실로 믿지 않는다.

## 검증

- 단순 코드 검토만으로 정상이라고 판단하지 않는다.
- 로컬 정적 서버로 실제 페이지를 열어 주요 화면과 상호작용을 확인한다.
- 로컬 서버 예시:

```powershell
python -m http.server 8899
```

- 검증이 끝나면 시작한 로컬 서버 프로세스를 종료한다.
- 데스크톱, 태블릿과 모바일 크기에서 레이아웃을 확인한다.
- 이미지 글자 자산 11개의 로딩 실패가 없는지 확인한다.
- 게임 자동 검증은 백그라운드 `requestAnimationFrame`에 의존하지 말고
  `step(1/60)`을 직접 반복한다.
- 배포 후에는 라이브 URL의 `/`, `/app-ads.txt`,
  `/games/soccer/` 응답을 확인한다.

## Git과 정리

- 사용자 작업이나 관련 없는 변경을 되돌리지 않는다.
- `git reset --hard`, 강제 checkout 같은 파괴적인 명령을 사용하지 않는다.
- `.gitignore`에 지정된 원본, 백업, `tmp/`, 검증 캡처와 구버전 자산을
  임의로 추적하지 않는다.
- 파일을 삭제하기 전 현재 코드에서 참조되는지 먼저 검색한다.
- 복구 가능한 삭제 방법을 우선하고, 삭제 후 대상과 복구 가능 여부를 알린다.
- 커밋에는 요청한 작업과 직접 관련된 파일만 포함한다.

## 규칙 문서 유지

- `CLAUDE.md`와 `AGENTS.md`는 같은 프로젝트 사실을 공유해야 한다.
- 파일 구조, 배포 방식, 브레이크포인트 또는 게임 규칙이 바뀌면 두 문서의
  관련 내용을 함께 갱신한다.
- 두 문서가 충돌하면 더 안전하고 더 구체적인 규칙을 따르고 사용자에게
  충돌 내용을 알린다.
